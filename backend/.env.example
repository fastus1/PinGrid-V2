# ⚠️ FICHIER D'EXEMPLE - NE PAS UTILISER DIRECTEMENT
#
# Copiez ce fichier vers .env et modifiez selon votre environnement:
# - Pour Docker Compose: voir .env.docker.example
# - Pour PostgreSQL Local: voir .env.local.example

# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pingrid
DB_USER=postgres
# IMPORTANT: Choisissez le bon mot de passe selon votre installation:
# - Docker Compose: DB_PASSWORD=postgres (voir .env.docker.example)
# - PostgreSQL Local: DB_PASSWORD=password (voir .env.local.example)
DB_PASSWORD=postgres

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Configuration
JWT_SECRET=pingrid-dev-secret-key-2024-change-in-production-very-long-secret
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
