# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration (PostgreSQL LOCAL)
# ⚠️ Ce fichier est pour une installation PostgreSQL LOCALE (non-Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pingrid
DB_USER=postgres
DB_PASSWORD=password    # ← Utilisez votre mot de passe PostgreSQL local

# Redis Configuration (optionnel en local)
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Configuration
JWT_SECRET=pingrid-dev-secret-key-2024-change-in-production-very-long-secret
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
